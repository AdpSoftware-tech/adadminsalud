class AppConfig {
  // Cambia el puerto si tu backend está en otro
  static const String baseUrl = 'http://localhost:3000';

  // Si luego necesitas /api, lo ponemos aquí:
  static const String apiPrefix = '/api';
}
